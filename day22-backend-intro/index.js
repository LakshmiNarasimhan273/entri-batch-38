// import express from "express"; library import in frontend
const express = require("express"); // library import in backend
const app = express();

app.listen(8081, () => {
    console.log('Server running on 8081');
})